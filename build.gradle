apply plugin: 'java'
apply plugin: 'application'
mainClassName = 'app.GUI'
jar {
    archiveBaseName = 'APP'
    archiveVersion =  '0.1.0'
    manifest {
    attributes(
     // 'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
      'Main-Class': 'app.GUI'
    )
  }
}